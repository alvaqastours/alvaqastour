backend:
  name: git-gateway
  branch: main  # replace with your GitHub branch name

media_folder: "assets/images"     # Folder where uploaded images are stored
public_folder: "/assets/images"   # Public path to images

site_url: "https://yourdomain.com" # Replace with your actual domain

collections:
  - name: "pages"
    label: "Pages"
    files:
      - label: "Packages Page"
        name: "packages"
        file: "packages.html"
        fields:
          - { label: "Page Title", name: "title", widget: "string" }
          - { label: "Header Image", name: "header_image", widget: "image", required: false }
          - { label: "Content", name: "body", widget: "markdown" }

      - label: "Services Page"
        name: "services"
        file: "services.html"
        fields:
          - { label: "Page Title", name: "title", widget: "string" }
          - { label: "Header Image", name: "header_image", widget: "image", required: false }
          - { label: "Content", name: "body", widget: "markdown" }
